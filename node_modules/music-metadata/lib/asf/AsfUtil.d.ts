/// <reference types="node" />
import { DataType } from "./AsfObject";
export declare type AttributeParser = (buf: Buffer) => boolean | string | number | Buffer;
export declare class AsfUtil {
    static getParserForAttr(i: DataType): AttributeParser;
    static parseUnicodeAttr(buf: any): string;
    /**
     * Best effort approach to read 64 but unsigned integer.
     * Note that JavasScript is limited to 2^53 - 1 bit.
     */
    static readUInt64LE(buf: Buffer, offset?: number): number;
    private static attributeParsers;
    private static parseByteArrayAttr(buf);
    private static parseBoolAttr(buf, offset?);
    private static parseDWordAttr(buf, offset?);
    private static parseQWordAttr(buf, offset?);
    private static parseWordAttr(buf, offset?);
}
